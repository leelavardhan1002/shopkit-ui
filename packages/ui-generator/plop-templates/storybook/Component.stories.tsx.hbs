import type { Meta, StoryObj, ArgTypes } from '@storybook/react';
import { {{pascalCase componentName}}, type {{pascalCase componentName}}Props } from '@shopkit/ui-core';

const argTypes: ArgTypes<{{pascalCase componentName}}Props> = {
    // Add your argTypes here
};

const meta: Meta<typeof {{pascalCase componentName}}> = {
    title: 'UI/{{pascalCase componentName}}',
    component: {{pascalCase componentName}},
    tags: ['autodocs'],
    parameters: {
        docs: {
            description: {
                component: 'A reusable UI component from the @shopkit/ui-core library.',
            },
        },
    },
    argTypes,
};

export default meta;
type Story = StoryObj<typeof meta>;

export const Default: Story = {
    args: {
        // Add your default args here
    },
};