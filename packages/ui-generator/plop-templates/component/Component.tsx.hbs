import { {{pascalCase componentName}}Props } from './types';
import { Styled{{pascalCase componentName}} } from './{{pascalCase componentName}}.style';

export const {{pascalCase componentName}}: React.FC<{{pascalCase componentName}}Props> = (props) => {
    return (
        <Styled{{pascalCase componentName}}>
            {{pascalCase componentName}}
        </Styled{{pascalCase componentName}}>
    );
};