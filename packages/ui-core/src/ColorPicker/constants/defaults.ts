import { ColorPickerProps } from '../types';

export const DEFAULT_COLOR_PICKER_PROPS: Partial<ColorPickerProps> = {
  variant: 'square',
  size: 'medium',
  showTooltip: false,
  showLabels: false,
  disabled: false,
  borderRadius: 0,
};
